import { SxProps } from "@mui/system";

const styles: {
  [key: string]: SxProps;
} = {
  wrapper: {
    padding: "50px 4%",
    alignContent: "center",
    alignItems: "center",
    display: "flex",
    flexDirection: "column",
    gap: "30px",
  },
};
export default styles;
